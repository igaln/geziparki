<!DOCTYPE html>
<html>
  <head>
    <title>{{ title }}</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <style>
    	a:link {color:#FF0000; font-size: 40px; margin-bottom: 50px;}      /* unvisited link */
		a:visited {color:#00FF00;}  /* visited link */
		a:hover {color:#FF00FF;}  /* mouse over link */
		a:active {color:#0000FF;}  /* selected link */
		#content {margin-bottom: 50px;}
		#sep {width:100%;height: 20px;background-color: black;}
    </style>
  </head>
  <body>
    <h1>BELOW ARE WHAT TURKISH PEOPLE SHARING THROUGH TWITTER LIVE </h1>
    <h2><a href="/?begin=0">REFRESH</a></h2>
   
    <div id="sep"></div>
    <ul>
	  {{#tweetdata}}
	  <li>
	  		
	  		<div id="content">
		  		{{#isImage}}
					 <img src="{{url}}" />
				{{/isImage}}
				{{#isVine}}
					 <iframe class="vine-embed" src="{{url}}/embed/simple" width="480" height="480" frameborder="0"></iframe><script async src="//platform.vine.co/static/scripts/embed.js" charset="utf-8"></script>
				{{/isVine}}
				{{#isTube}}
				<iframe width="420" height="315" src="{{url}}" frameborder="0" allowfullscreen></iframe>
				{{/isTube}}
				{{^isImage}}
				  {{^isVine}}
				  	{{^isTube}}
				 	  <a href="{{url}}">{{text}}</a>
				 	 {{/isTube}}
					{{/isVine}}
				{{/isImage}}
				
	  		</div>
	  </li>
	  {{/tweetdata}}
	</ul>
	 <div id="sep"></div>
	<DIV> <a href="/?begin={{start}}"> NEXT || DEVAM </a>

  </body>
</html>